# Environment variables / secrets (root and config folder)
.env
.env.*
config/.env
config/.env.*
# ...but keep example/template env files
!.env.example

# Local runtime data (MongoDB files, uploaded images)
data/

# Database backups
*.tar.gz

# Logs
logs
*.log
